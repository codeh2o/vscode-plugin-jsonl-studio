{
  "name": "better-jsonl-preview",
  "displayName": "Better JSONL Preview",
  "description": "Preview and safely edit JSONL and NDJSON files in a formatted side-by-side view.",
  "version": "0.0.1",
  "publisher": "landz",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/codeh2o/Clipboard-Format-Preview.git"
  },
  "bugs": {
    "url": "https://github.com/codeh2o/Clipboard-Format-Preview/issues"
  },
  "homepage": "https://github.com/codeh2o/Clipboard-Format-Preview#readme",
  "icon": "media/icon.png",
  "engines": {
    "vscode": "^1.88.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:better-jsonl-preview.openJsonlPreview"
  ],
  "main": "./out/src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "better-jsonl-preview.openJsonlPreview",
        "title": "Open Better JSONL Preview",
        "icon": "media/preview-format-title.svg"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "better-jsonl-preview.openJsonlPreview",
          "when": "resourceExtname == .jsonl || resourceExtname == .ndjson",
          "group": "navigation@100"
        }
      ],
      "commandPalette": [
        {
          "command": "better-jsonl-preview.openJsonlPreview",
          "when": "false"
        }
      ]
    }
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('out',{recursive:true,force:true})\"",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "npm run clean && npm run compile && node --test out/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@types/vscode": "^1.88.0",
    "typescript": "^5.4.5"
  }
}
